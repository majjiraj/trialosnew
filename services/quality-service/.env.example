# Quality Service — copy to .env for local development
# Standalone: use the docker-compose.yml postgres instance
DATABASE_URL=postgresql://quality:quality_dev@localhost:5432/quality_db

# Platform integration: use the shared trialo postgres (run from repo root docker-compose.dev.yml)
# DATABASE_URL=postgresql://trialo:trialo_dev@postgres:5432/quality_db

SERVICE_PORT=8016
LOG_LEVEL=INFO
